---
name: devagotchi-reset
description: Reset your Devagotchi — release it and hatch a new one.
disable-model-invocation: true
allowed-tools: Bash(node:*) Bash(bash:*)
---

Delete all progress and hatch a new pet.

Run: `bash ${CLAUDE_SKILL_DIR}/../scripts/reset.sh`

Display the raw output exactly as printed.
