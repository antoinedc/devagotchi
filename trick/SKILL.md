---
name: devagotchi-trick
description: Watch your Devagotchi perform an ASCII trick.
disable-model-invocation: true
allowed-tools: Bash(node:*) Bash(bash:*)
---

Watch your pet perform a fun ASCII trick.

Run: `bash ${CLAUDE_SKILL_DIR}/../scripts/trick.sh`

Display the raw output exactly as printed.
